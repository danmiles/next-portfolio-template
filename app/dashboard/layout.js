
export const metadata = {
  title: 'JsCase | Dashboard',
  description:
    'Dashboard page of JsCase. We create digital products for your business.',
};

export default function Layout({ children }) {
  return <>{children};</>;
}
